macros:
  - name: cents_to_dollars
    description: A macro that converts dollars to cents
    arguments:
      - name: column_name
        type: string
        description: The name of column to convert
      - name: decimals
        type: integer
        description: number of decimal places, default 2